<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.buahbatu.sabang17">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_hmi"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity" android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" android:label="@string/app_name"/>
        <activity android:name=".InputCriticActivity" android:label="@string/title_aspiration"/>
        <activity android:name=".InfoDetailActivity" android:label="@string/title_detail_event"/>
        <activity android:name=".BusinessDetailActivity" android:label="@string/title_detail_goods"/>
        <activity android:name=".CriticDetailActivity" android:label="@string/title_detail_aspiration"/>
    </application>

</manifest>